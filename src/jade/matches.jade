ion-content
    ion-refresher(
        pulling-text="Pull to refresh..."
        on-refresh="refresh()"
    )
    ion-item(collection-repeat='r in matches')
        div
            | {{r.home_club.name}} VS {{r.away_club.name}}
