ion-side-menus(enable-menu-with-back-views='false')
    ion-side-menu-content
        ion-nav-bar.bar-stable
            ion-nav-back-button
            ion-nav-buttons(side='left')
                button.button.button-icon.button-clear.ion-navicon(menu-toggle='left')

        // content section
        ion-nav-view(name='content')

    ion-side-menu(side='left')
        ion-header-bar.bar-stable(ng-show="IsLoggedIn")
            h1.title Left {{IsLoggedIn}}

        ion-content
            ion-list
                ion-item(menu-close='', href='#')
                    | HOME FEED
                ion-item(menu-close='', href='#/app/news')
                    | NEWS
                ion-item(menu-close='', href='#/app/matches')
                    | MATCHES
                ion-item.header(menu-close='')
                    | LEAGUES
                ion-item(ng-repeat="menu in menus.leagues", menu-close='', href="{{menu.link}}")
                    | {{menu.text}}
                ion-item(menu-close='', href='#')
                    | PEOPLE RANKING
                ion-item(menu-close='', href='#')
                    | FANZONE
                ion-item(menu-close='', href='#')
                    | REWARD
                ion-item(menu-close='', ng-click='login()')
                    | Login
            ion-list(ng-show="IsLoggedIn")
                ion-item.header(menu-close='')
                    | {{menus.setting.header.text}}
                ion-item(ng-repeat="menu in menus.leagues.items")
                    | {{menu.text}}
                ion-item(menu-close='', href='#')
                    | MORE
                ion-item(menu-close='', ng-click="")
                    | LOG OUT
                //- ion-item(menu-close='', href='#/app/search')
                //-     | Search
                //- ion-item(menu-close='', href='#/app/browse')
                //-     | Browse
                //- ion-item(menu-close='', href='#/app/playlists')
                //-     | Playlists
                //TODO: beer may be edit the collapse ;[[
                //ion-item.item-stable(ng-click="toggleGroup(group)",ng-class="{active: isGroupShown(group)}")
                //    i.icon(ng-class="isGroupShown(group) ? 'ion-minus' : 'ion-plus'")
                //    | test
                //ion-item.item-accordion(ng-repeat="item in menus", ng-show="isGroupShown(group)")
                //    | {{item.text}}
